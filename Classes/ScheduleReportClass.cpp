/*This class "prints" displays the Schedule Reports to be emailed or printed to
the staff firm requesting the report.*/

#include <string>
#include <iostream>
#include <ScheduleClass.cpp>
using namespace std;

	class ScheduleReport {

	string schedule.getprintReport(string printReport) {
		
			return printReport();
		}
	};
